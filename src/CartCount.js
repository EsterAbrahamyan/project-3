// import React from "react";

// function CartCount(props) {
//   const uniqueItems = new Set(props.cart.map(item => item.id));
//   const count = uniqueItems.size;
//   return <div>
//  Ընդհ․քանակ: {count}</div>;
// }

// export default CartCount;
